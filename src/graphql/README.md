# GraphQL Operations

`operations.ts` contains the GraphQL query and mutation strings executed by the SDK client.

The backend owns the GraphQL schema and business logic. This SDK only wraps backend calls with typed public methods in `src/types/public.ts`.
